# ASDF runtimes manager
source "$(brew --prefix asdf)/libexec/asdf.sh"

# rbenv
# eval "$(rbenv init -)"

# pyenv
# eval "$(pyenv init --path)"
# eval "$(pyenv init -)"
# eval "$(pyenv virtualenv-init -)"

# nodenv
# eval "$(nodenv init -)"
